/**
 * Placeholder app shell for Phase 1 (scaffold).
 *
 * This confirms the toolchain and brand tokens are wired up. The real header,
 * category sections, horizon columns, cards and footer arrive in Phase 3
 * (docs/BUILD_BRIEF.md Section 12).
 */
export default function App() {
  return (
    <main className="mx-auto max-w-3xl px-4 py-16">
      <p className="text-sm font-medium uppercase tracking-wide text-dhcw-blue">
        Digital Health and Care Wales
      </p>
      <h1 className="mt-2 text-3xl font-bold text-navy">
        Primary, Community and Mental Health: our roadmap
      </h1>
      <p className="mt-4 text-ink-700">
        Scaffold ready. Content model, layout and components follow in later
        phases.
      </p>
    </main>
  );
}
